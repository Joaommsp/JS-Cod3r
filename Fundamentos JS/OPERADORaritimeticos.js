const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d // as operaçoes são binárias ( de 2 em 2  operados)
console.log(soma)

const sub = a - b

const mult = a * d

const div = d - a

const mod = a % 2 // usado para saber se um número é par ou ímpar

console.log(sub, mult, div, -mod) // -mod usando operaod unário ( em apenas 1 operado)
